spring:
  profiles:
    active: dev
  redis:
    host: localhost
    port: 6379
  boot:
    admin:
      client:
        url: http://127.0.0.1:9090
management:
  server:
    port: 9092
  endpoints:
    web:
      exposure:
        include: '*'
      base-path: /manage
#自定义信息
info:
  auth:
    name: lct
    dep: dev
debug: true